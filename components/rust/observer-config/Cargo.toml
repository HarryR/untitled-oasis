[package]
name = "observer-config"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.27", default-features = false, features = ["derive"] }

[features]
observer-localnet = []
observer-testnet = []
observer-mainnet = []
